// Marcador de versão pedido pelo Felipe (sessão G, 14/09) — só pra bater o
// olho e confirmar na hora se o navegador está rodando o código novo ou um
// antigo ainda na memória/cache, sem precisar de grep ou git log. Atualizo
// essa string manualmente a cada entrega nova; se o rodapé do painel
// mostrar uma versão diferente da que eu acabei de mandar, é sinal de que o
// deploy/reload não pegou — não precisa adivinhar mais.
export const BUILD_VERSION = '2026-09-17-sessao-l-01';
